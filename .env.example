DISCORD_TOKEN=
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# Google OAuth (web login via apps/backend)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Backend session / routing
DCB_SESSION_SECRET=
DCB_UI_BASE=
DCB_PUBLIC_URL=

# ── Solana ──────────────────────────────────────────────
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
SOLANA_PRIVATE_KEY=
SOLANA_PUBLIC_KEY=
CLUSTER=mainnet-beta

# ── End-to-End Encryption of Private Keys ──────────────
# 64 hex chars (256-bit). MUST match between bot and backend.
# Generate with:  node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=

# Optional separate key for encrypting secrets in transit between bot <-> backend.
# If not set, ENCRYPTION_KEY is used for both at-rest and transit encryption.
# 64 hex chars (256-bit).
E2E_TRANSPORT_KEY=

# ── Other Crypto ────────────────────────────────────────
ETH_PRIVATE_KEY=
INFURA_URL=
BTC_API=
LIGHTNING_API=
